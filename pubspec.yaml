name: orthoviz
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter_inappwebview: ^6.1.5
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.1.0 # Add this line

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/orthoviz/ortho.html
    - assets/orthoviz/ortho.js
    - assets/orthoviz/ortho.pck
    - assets/orthoviz/ortho.wasm
    - assets/orthoviz/ortho.side.wasm
    - assets/orthoviz/ortho.audio.worklet.js
    - assets/orthoviz/ortho.audio.position.worklet.js
    - assets/orthoviz/ortho.icon.png
    - assets/orthoviz/ortho.png
    - assets/orthoviz/ortho.apple-touch-icon.png
    - assets/orthoviz/flutter_bridge.js   # ← added